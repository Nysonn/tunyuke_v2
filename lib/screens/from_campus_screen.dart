import 'package:flutter/material.dart';

class FromCampusScreen extends StatelessWidget {
  const FromCampusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('From Kihumuro Campus')),
      body: const Center(child: Text('Details for From Kihumuro Campus ride.')),
    );
  }
}
