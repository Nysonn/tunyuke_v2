import 'package:flutter/material.dart';

class ToCampusScreen extends StatelessWidget {
  const ToCampusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('To Kihumuro Campus')),
      body: const Center(child: Text('Details for To Kihumuro Campus ride.')),
    );
  }
}
